{
  "name": "@noctiv/site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.ts",
    "dev": "node build.ts --serve",
    "shots": "node scripts/shots.ts",
    "typecheck": "tsc -p tsconfig.json",
    "check": "node scripts/check.ts"
  },
  "devDependencies": {
    "@fontsource-variable/manrope": "^5.3.0",
    "playwright-core": "^1.63.0"
  }
}
